#' Multiply together two numbers.
#'
#' @param x A number.
#' @param y A number.
#' @return The product of \code{x} and \code{y}.
#' @examples
#' mult(1, 1)
#' mult(0.5, 0.26)
#' mult(exp(1), pi)
#' mult(-0.5, 1)
#' @export
mult <- function(x, y) {
    return(x * y)
}
