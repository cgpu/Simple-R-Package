#' Add together two numbers.
#'
#' @param x A number.
#' @param y A number.
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' add(1, 1)
#' add(2, 3)
#' add(0.5, 3.5)
#' add(-0.5, -0.8)
#' @export
add <- function(x, y) {
    return(x + y)
}
